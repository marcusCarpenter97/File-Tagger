#include "../src/database_manager.c"
#include "../src/database_manager.h"

char* tag_name1 = "tag name 1";
char* tag_name2 = "tag name 2";

char* location1 = "path/to/loc1"; 
char* location2 = "path/to/loc2"; 

#test insert_one_tag
	append_path_to_sql_stmt(location1);
	append_tag_name_to_sql_stmt(tag_name1);
	int ret_val = add_tags_to_db();
	fail_unless(ret_val == EXIT_SUCCESS);

#test insert_many_tags
	append_path_to_sql_stmt(location1);
	append_path_to_sql_stmt(location2);
	append_tag_name_to_sql_stmt(tag_name1);
	append_tag_name_to_sql_stmt(tag_name2);
	int ret_val = add_tags_to_db();
	fail_unless(ret_val == EXIT_SUCCESS);
